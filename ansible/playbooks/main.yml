---

- name: Deploy all
  hosts: all
  gather_facts: false

  tasks:

    - name: Deploy nginx
      include_tasks:
        file: nginx-docker.yml
        apply:
          tags:
            - nginx
      tags:
        - nginx

    - name: Deploy ghost
      include_tasks:
        file: ghost-docker.yml
        apply:
          tags:
            - ghost
      tags:
        - ghost
